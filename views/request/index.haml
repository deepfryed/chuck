- if resource.rows > 0
  %table.table.requests
    %thead
      %tr
        %th ID
        %th Method
        %th URI
        %th Started
        %th Status
        %th Lasted
        %th
    %tbody
      - resource.each do |request|
        != haml :'request/_request', locals: {request: request}
- else
  %img{src: '/img/sad-cat.jpg'}
  .empty
    I EATD TEH LOGS, GO FETCH MOAR
